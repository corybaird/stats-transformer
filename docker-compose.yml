services:
  r-integration:
    build:
      context: .
      dockerfile: tests/replication/r_packages/Dockerfile
    volumes:
      - .:/app
    environment:
      - R_AVAILABLE=1
    command: >
      bash -c "uv sync && uv run python -m pytest tests/replication/r_packages/ -v"
